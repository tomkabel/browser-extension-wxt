# DEPRECATED — Do not use

This directory is the legacy Android project structure. All active development
has moved to `vault-android/` at the repository root.

This directory is retained for reference only. Do not add new code here.

See `PROJECT_OWNERSHIP.md` in the repository root for the full directory map.
