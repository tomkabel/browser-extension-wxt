schema: spec-driven
created: 2026-05-03
updated: 2026-05-04
specs:
  - challenge-derivation
  - webauthn-assertion-binding
  - passkey-provisioning
  - challenge-recomposition-engine
context: |
  Phase 2 cryptographic binding of TLS binding proof (progressive three-tier:
  Tier 1 Sec-Fetch headers, Tier 2 Token Binding, Tier 3 DECO WASM oracle),
  transaction origin, control code, and session nonce into SHA-256 WebAuthn
  challenge (version 0x02).
v6_alignment: |
  PHASE 2 — Core V6 cryptographic layer. Replaces the deprecated monolithic
  zkTLS proof with a progressive three-tier TLS binding system. Tier 1 works
  today with zero infrastructure. Depends on usb-aoa-transport-proxy
  (complete) and tls-binding-engine (was zktls-context-engine) for proof
  generation.
